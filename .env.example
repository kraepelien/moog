# Copy to .env on the machine that runs the app. Never committed: see .gitignore.
#
# With PM_OAUTH_CLIENT_ID unset the app runs as a single local user and
# nothing is gated, which is how development and the test suite run.

# From Google Cloud Console → APIs & Services → Credentials → OAuth client ID
# (Web application). A Web client matches the redirect URI exactly, port and all,
# so every origin you sign in from has to be registered:
#   https://patchmemory.app/api/auth/google/callback
#   http://localhost:5173/api/auth/google/callback
# Several worktrees at once means several ports — the dev server takes the next
# one free — so register the range you use, 5173 through 5179 or so.
PM_OAUTH_CLIENT_ID=
PM_OAUTH_CLIENT_SECRET=

# Any long random string. Changing it signs everybody out.
PM_SESSION_SECRET=

# Where the app is reached, used to build the redirect URI. Required wherever it
# is deployed, because a request's own Host is forgeable — and left EMPTY on a
# development machine, though it does no harm either way: a request to localhost
# answers as itself whatever this says, so every worktree's port works and none
# of them sends you to production.
PM_PUBLIC_ORIGIN=

# Comma-separated emails, and the whole guest list: an address that is not here
# is turned away at the callback and never gets a row. Leave it empty with
# PM_OAUTH_CLIENT_ID set and nobody can sign in at all.
PM_ADMINS=

# The caps, with their defaults shown. Written for open registration and kept
# against a guest list, because they cost nothing until the door opens wider.
# Leave them unset unless somebody actually meets one.
PM_MAX_PATCHES=2000
PM_MAX_PATCH_BYTES=65536
PM_TRASH_DAYS=30
PM_WRITES_PER_MINUTE=120
PM_SIGN_INS_PER_MINUTE=10
